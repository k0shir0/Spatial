# Browser preference

- Use the in-app browser for browser-based tasks by default.
- Do not use or launch Google Chrome through Chrome-control automation unless the user explicitly requests it. The automated Chrome launch currently crashes during macOS application registration.
